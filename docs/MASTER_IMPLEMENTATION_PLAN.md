# QuantumLab source reconciliation and research plan

## Scope

Preserve existing dirty work and the canonical registry/worker/API/Book Lab flow. Book evidence comes from the accessible McMahon 2008 PDF, ISBN 978-0-470-09699-4. External algorithms and surface-code research remain separate. No push is authorized. Commit only after verification and secret review.

## Source inventory

Use printed page references from source body and TOC, keeping extraction ambiguities explicit. Create documentation data, not another registry. Inventory sections, 119 numbered worked examples, 126 top-level exercises and 34 body You Try It prompts; subparts require individual reconciliation before exhaustive substantive coverage can be claimed. Map each item to actual primitive, runner, UI and independent oracle; do not inherit old VALIDATED labels.

## Bounded implementation slices

1. Core algorithms: repair Simon low-register observations/rank acquisition; extend existing Grover marked-set support without a second algorithm engine. Preserve old supported calls while migrating repository callers to clear contracts.
2. QFT/QPE: reconcile cutoff exponent with angle convention; support arbitrary eigenstate preparation through existing StateVector simulator. Add direct bounded Book Lab studies with independent DFT/phase probability references.
3. MBQC: actual linear-cluster equatorial measurements, outcome-conditioned second measurement and Pauli correction, exhaustive nonzero branches against an independently composed unitary. Reuse graph/state primitives; no compiler or general MBQC language.
4. Book correctness: coherent logical superposition QEC, corrected-target swapping, common-channel trace-distance/fidelity contraction; preserve raw/projected tomography and sampled-grid Rabi semantics.
5. Integration owner: existing book_runner wrappers, RUNNER_REGISTRY entries, BookLab metadata and real browser tests; expose explicit seed, honest results and limits without a new page/runner framework.

## D7 prerequisite corrections and study

Delayed signature history must preserve injection transient; event comparisons must use absolute detection layers. Regression reproduction precedes each fix. Preserve known M19/M20 mechanisms. Audit forced-fault composition and sampled channel probabilities before interpreting likelihoods. Measure exact signature generation at d=3/5/7, baseline and fitted_pair extraction, distinguishing Python allocations from process RSS. Validate every enumerated mechanism at specified injection placements; report any unsolved faults rather than suppressing them. Run paired decoder trials on identical circuit samples with Wilson intervals, per-decoder timing and explicit seeds. fitted_pair is a circuit extraction decomposition, not a trained numerical fit. Do not revive closed two-fault optimization or claim a threshold.

## Verification and delivery

Focused regressions after each slice, full backend and frontend checks once integrated, real process-worker/API/persistence/browser execution, representative visual inspection and failure handling. Audit secrets without publishing sensitive values. Update README, coverage matrix, research report, roadmap and handoff with exact commands/results and explicit unresolved scope. Final report evaluates every definition-of-done item; commit coherent verified changes, then inspect final tree and run post-commit smoke. No incomplete work receives a COMPLETE or production-ready label.
